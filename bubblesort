#include <stdio.h>

int main() {
    int a[5]={5,7,3,9,0};
    int i=0;
    int temp;
    while(i<5){
    for (int j=0;j<5;j++){
        if (a[j+1]<a[j]){
            temp=a[j+1];
            a[j+1]=a[j];
            a[j]=temp;
            
        }
       
    }
    i=i+1; 
    }
for(int k=0;k<5;k++){
    printf("%d\n",a[k]);
}    
    
    return 0;
}
